<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AnnelidsClient</title>
    <script>
        const connect = async (key) => {
            const req = await fetch(`/connect?key=${key}`)
            location.reload()
        }
    </script>
</head>
<body>
    <%- Object.keys(games).map(game => 
        `<a onclick="connect('${game}')" href="#">"${game}" - ${games[game].address}:${games[game].port}</a>`) 
    %>
</body>
</html>